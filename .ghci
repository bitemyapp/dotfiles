:def hoogle \s -> return $ ":! hoogle --count=15 \"" ++ s ++ "\""
:def doc \x -> return $ ":!hoogle --info \"" ++ x ++ "\""

:set -fno-warn-type-defaults
-- :set -fobject-code
:def source readFile
-- :set -fdefer-type-error
:set prompt "Prelude> "

-- let numPlus = (+)
-- let x + y = numPlus (numPlus x y) 1
